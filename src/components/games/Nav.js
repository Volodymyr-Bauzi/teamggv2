import classes from './Nav.module.css';
import React from 'react';

function Nav() {
   return (
      <div className={classes.filter}>
         <h1>Фильтры</h1>
      </div>
   );
}

export default Nav;
